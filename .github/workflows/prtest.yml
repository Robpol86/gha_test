name: PR Test

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - closed

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      - run: jq -S . < "$GITHUB_EVENT_PATH"
      - run: |
          curl -fI https://api.github.com/repos/$GITHUB_REPOSITORY/pulls/2
